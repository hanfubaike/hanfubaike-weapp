<view>
<image-cropper id="image-cropper" limit_move="{{true}}" disable_rotate="{{true}}" width="{{width}}" height="{{height}}" imgSrc="{{src}}" bindimageload="loadimage" bindtapcut="clickcut" style="z-index: 1111;" disable_ratio="{{true}}" quality="{{quality}}" export_scale="{{export_scale}}"></image-cropper>

<view class='bottom'>

  <van-button bind:click="submit" style="padding-right: 30px;" type="primary" size="small">确定</van-button>
  <van-button bind:click="back" type="primary" size="small">取消</van-button>
</view>
</view>